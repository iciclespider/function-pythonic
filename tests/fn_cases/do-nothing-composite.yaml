request:
  observed:
    composite:
      resource:
        apiVersion: pytest.crossplane.io/v1alpha1
        kind: Step
        spec:
          composite: |
            class DoNothingComposite(BaseComposite):
              def compose(self):
                pass
  input:
    step: pytest
    inlined: composite
