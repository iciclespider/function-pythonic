request:
  observed:
    composite:
      resource:
        apiVersion: pythonic.crossplane.io/v1alpha1
        kind: Composite
        spec:
          composite: |
            class DoNothingComposite(BaseComposite):
              def compose(self):
                pass
  input:
    step: pytest
